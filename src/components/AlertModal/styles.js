import styled from 'styled-components';

export const Container = styled.div`
    .modal {
        h2 {
            font-size: 1.3rem;
            color: var(--text-body);
            margin: 20px 20px;
            font-weight: 600;
        }
    
        h3 {
            font-size: 1.1rem;
            color: var(--text-body);
            font-weight: 400;
            color: red;
        }   
    }
`;